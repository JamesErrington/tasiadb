{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make build"
        },
        {
            "label": "build-test",
            "type": "shell",
            "command": "make build_test file=${file}"
        }
    ]
}
